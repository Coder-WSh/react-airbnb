import PropTypes from 'prop-types'
import React, { memo } from 'react'
import FooterStyled from './styled'

const FooterBody = memo((props) => {
  return (
    <FooterStyled>
        FooterBody
    </FooterStyled>
  )
})

FooterBody.propTypes = {}

export default FooterBody